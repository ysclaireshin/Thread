# Anthropic API key used by the Replay AI summary (Flow).
# Read server-side only by the /api/replay proxy in vite.config.ts.
# It is deliberately NOT prefixed with VITE_, so it never reaches the browser bundle.
# Copy this file to .env.local and paste your key. Without it, Replay falls back gracefully.
ANTHROPIC_API_KEY=
